services:
  - type: web
    name: comicify
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --timeout 600 --workers 1
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
